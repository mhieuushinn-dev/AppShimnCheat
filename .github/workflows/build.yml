      - name: Verify App
        run: |
          set -e

          APP_PATH="$PWD/build/Release-iphoneos/ShinnCheat.app"

          echo "======================================"
          echo "VERIFY APP"
          echo "======================================"

          if [ ! -d "$APP_PATH" ]; then
            echo "❌ Không tìm thấy:"
            echo "$APP_PATH"

            find "$PWD/build" -maxdepth 5 -print || true

            exit 1
          fi

          echo "✅ ShinnCheat.app FOUND"

          if [ ! -f "$APP_PATH/Info.plist" ]; then
            echo "❌ Info.plist vẫn chưa được tạo"
            exit 1
          fi

          echo "✅ Info.plist FOUND"

          ls -la "$APP_PATH"